import { PageStub } from './PageStub';

export function Run() {
  return (
    <PageStub
      eyebrow="Activité courante non rattachée à un projet"
      titre="Run"
      message="Le module Run (liste, kanban, points hors projet) arrive au Lot 6."
    />
  );
}
